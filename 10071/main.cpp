#include <bits/stdc++.h>
using namespace std;

// Time complexity: O(1)
// Space complexity: O(1)
int main() {
    int v, t;
    while (scanf("%d %d", &v, &t) != EOF) {
        cout << 2 * v * t << endl;
    }
    return 0;
}
